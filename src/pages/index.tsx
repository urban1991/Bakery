import {PageHead} from "$/components/PageHead";
import {Home} from "$/components/Home";

export default function HomePage() {
    return (
        <>
            <PageHead title="Strona główna" />

            <Home />
        </>
    );
}
